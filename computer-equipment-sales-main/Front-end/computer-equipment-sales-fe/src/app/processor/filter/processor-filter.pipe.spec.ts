import { ProcessorFilterPipe } from './processor-filter.pipe';

describe('ProcessorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
