import { DisplayCardFilterPipe } from './display-card-filter.pipe';

describe('DisplayCardFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayCardFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
