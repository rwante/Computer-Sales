import { HddFilterPipe } from './hdd-filter.pipe';

describe('HddFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HddFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
