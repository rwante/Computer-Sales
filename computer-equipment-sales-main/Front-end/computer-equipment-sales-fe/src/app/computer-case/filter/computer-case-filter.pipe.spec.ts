import { ComputerCaseFilterPipe } from './computer-case-filter.pipe';

describe('ComputerCasePipe', () => {
  it('create an instance', () => {
    const pipe = new ComputerCaseFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
