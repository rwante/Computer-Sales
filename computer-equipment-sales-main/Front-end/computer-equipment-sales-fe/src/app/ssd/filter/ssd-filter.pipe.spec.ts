import { SsdFilterPipe } from './ssd-filter.pipe';

describe('SsdFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SsdFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
