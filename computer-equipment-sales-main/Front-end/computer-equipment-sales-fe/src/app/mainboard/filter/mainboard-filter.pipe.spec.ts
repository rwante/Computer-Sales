import { MainboardFilterPipe } from './mainboard-filter.pipe';

describe('MainboardFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MainboardFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
