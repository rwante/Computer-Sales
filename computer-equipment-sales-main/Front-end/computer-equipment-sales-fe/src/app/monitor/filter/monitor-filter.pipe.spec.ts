import { MonitorFilterPipe } from './monitor-filter.pipe';

describe('MonitorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MonitorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
