import { LaptopFilterPipe } from './laptop-filter.pipe';

describe('LaptopFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LaptopFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
